/*
a = the first element in the series
r = the ratio between every element and the privious ans has to be constant

to infinty:
sum = a / (1-r)
-> 0 <= r < 1.

to m:
sum = (a * (1 - r^m))  / (1-r).qZ0
*/